import "isomorphic-fetch";

// import {_activate} from "../src/index";

describe("Checks activate", () => {
  test("Check activate", () => {
    // expect(_activate);
    expect(true).toBe(true);
  });
});
